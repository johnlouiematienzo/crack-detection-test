import MainContent from "@/app/components/MainContent";

export const metadata = {
  title: "Object Classification Demo",
  description:
    "Demo for classification of objects with pre-trained models using OpenCV, TensorFlow, etc.",
  icons: {
    icon: "/favicon.png",
  },
};

const Home = () => {
  return <MainContent />;
};

export default Home;
